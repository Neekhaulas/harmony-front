{{#each this.users as |user|}}

<div class="user">
    <div class="user-avatar">
        <img class="avatar" src="https://eu.ui-avatars.com/api/?name={{user.username}}" />
    </div>
    <div>{{user.username}}</div>
</div>
{{/each}}